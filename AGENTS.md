# AGENTS.md — The Understory 開発エージェント向け手順書

このリポジトリは「The Understory（棄てられたものの国）」Minecraft MOD の設計・開発リポジトリ。
どのエージェントセッションでも、以下の手順で必ず同一のコンテキストを復元してから作業すること。

## セッション開始時（必須、順番厳守）

1. **`STATE.md` を読む** — 現フェーズ・完了済み・次の作業・未決事項が1ページに集約されている。
2. **GitHub Issues を確認** — `gh issue list` でオープンなチケットを一覧。担当チケットが指定されていればそれを読む。各チケットには完了条件と参照ドキュメントが書いてある。
3. **関連ドキュメントを読む** — チケットの「参照」に挙がっている `docs/` を読む。方針の根拠が必要なら `docs/30_adr/` のADRを読む。
4. 上記が済むまで実装・設計の判断をしない。

## セッション終了前（必須）

1. **担当Issueに進捗コメントを残す** — 何をやったか・何が残っているか・次のエージェントへの申し送り。
2. **`STATE.md` を更新する** — 「次にやるべきこと」「未決事項」「直近の変更」を常に最新に保つ。これが引き継ぎの生命線。
3. 変更をコミットして push する。設計ドキュメントの追加・変更もすべて git 管理。

## 設計・決定の記録ルール

- **重要な決定はADRにする**（`docs/30_adr/`）。テンプレートは `docs/30_adr/README.md`。ADRは追記修正しない——覆すときは新しいADRで「supersedes」と書く。
- **未決・要調査事項は GitHub Issue にする**。todo.md は Issue への案内役（個人メモ的な残件だけ記載）。
- ユーザー（hi-lee-mon / 65banseki）の承認が必要な決定（方向性・範囲・公開物）は Issue や STATE.md に「要ユーザー承認」と明記する。

## プロジェクトの不変条件

- 大方針は「徹底設計」：実装前に設計・レビューを尽くす（`docs/00_development_plan.md`）。
- 現フェーズは **Phase 0（設計）**。実装コードの作成は Phase 1 以降、ユーザーの明示的な許可があるまで行わない。
- ターゲットは YouTuber/VTuber。配信映え・章＝エピソード構造は常に意識する（`docs/17_streaming_design.md`）。
- 技術選定：NeoForge / MC 1.21.1 / Java 21（`docs/30_adr/0002_tech_stack.md`）。
- 回答・ドキュメント・コメントは**日本語**（コード識別子は英語）。
